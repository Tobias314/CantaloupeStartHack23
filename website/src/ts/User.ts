export interface User {
    id: string
    level: number
    paidToday: number
    paidTotal: number
    timePlayedToday: number
    timePlayedTotal: number
    existingFor: number
    meanRiskToday: number
}
